package main

import (
	"fmt"
	"os"

	"anything-tui/tui"

	tea "github.com/charmbracelet/bubbletea"
)

func main() {
	m := tui.NewApp()

	p := tea.NewProgram(m, tea.WithAltScreen())

	if _, err := p.Run(); err != nil {
		fmt.Printf("its all fucked %v", err)
		os.Exit(1)
	}
}
